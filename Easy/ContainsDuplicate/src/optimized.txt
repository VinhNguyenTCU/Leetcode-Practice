1: Get the length of the array

2: If the array contains only one element -> return false

3: If the array contains only two elements -> Check if those two elements are equal
If they are equal, return true else return false

4: Use the insertion sort and then iterate through the arrays 